import Nullstack from "nullstack"

class HelloWorld extends Nullstack {

  render () {
    return (
      <div>
        <h1>Hello, World!</h1>
        <p>This feels a lot like React.</p>
      </div>
    )
  }
}

export default HelloWorld
