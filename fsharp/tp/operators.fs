(* arithmetic, boolean and binary operators *)

let a: int32 = 21
let b: int32 = 10

printfn "~ arithmetic operators ~"
printfn "a + b = %d" (a + b)
printfn "a - b = %d" (a - b)
printfn "a * b = %d" (a * b)
printfn "a / b = %d" (a / b)
printfn "a %% b = %d" (a % b)
printfn "a ** b = %g" ((float a) ** (float b))

